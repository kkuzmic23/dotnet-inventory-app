<Window x:Class="WPFLayer.ImportDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import details" Height="500" Width="700">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <TextBlock Name="txtSupplierName" FontSize="18" FontWeight="DemiBold" />
            <TextBlock Text="Aggregated totals" Margin="0,6,0,2" FontWeight="Bold" />
            <ItemsControl Name="icTotals">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <DockPanel Margin="0,2,0,0">
                            <TextBlock DockPanel.Dock="Left" Text="{Binding ProductName}" />
                            <TextBlock DockPanel.Dock="Right" Text="{Binding TotalQuantity, StringFormat=Amount: {0}}" FontWeight="Bold" />
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <TextBlock DockPanel.Dock="Top" Text="Order items" FontWeight="Bold" Margin="0,4,0,4" />
        <DataGrid Name="dgDetails" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order Id" Binding="{Binding OrderId}" />
                <DataGridTextColumn Header="Status" Binding="{Binding OrderStatus}" />
                <DataGridTextColumn Header="Created" Binding="{Binding OrderCreatedAt, StringFormat=\{0:yyyy-MM-dd\}}" />
                <DataGridTextColumn Header="Received" Binding="{Binding OrderReceivedAt, StringFormat=\{0:yyyy-MM-dd\}}" />
                <DataGridTextColumn Header="Product Code" Binding="{Binding ProductCode}" />
                <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Name="btnImport" Content="Import" Width="80" Height="26" Margin="0,0,6,0" Click="btnImport_Click" />
            <Button Name="btnClose" Content="Close" Width="80" Height="26" Click="btnClose_Click" />
        </StackPanel>
    </DockPanel>
</Window>
