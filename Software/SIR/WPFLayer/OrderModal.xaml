<Window x:Class="WPFLayer.OrderModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLayer"
        mc:Ignorable="d"
        Title="OrderModal" Height="450" Width="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Supplier" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <ComboBox x:Name="cmbSupplier" Grid.Row="0" Grid.Column="1" Margin="0,0,12,8"/>

            <TextBlock Grid.Row="0" Grid.Column="2" Text="Status" VerticalAlignment="Center" Margin="0,0,8,8"/>
            <TextBox x:Name="txtStatus" Grid.Row="0" Grid.Column="3" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Created At" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker x:Name="dpCreatedAt" Grid.Row="1" Grid.Column="1" Margin="0,0,12,0"/>

            <TextBlock Grid.Row="1" Grid.Column="2" Text="Received At" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker x:Name="dpReceivedAt" Grid.Row="1" Grid.Column="3"/>
        </Grid>

        <DataGrid x:Name="dgItems" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridComboBoxColumn x:Name="colProduct"
                                    Header="Product"
                                    DisplayMemberPath="Name"
                                    SelectedItemBinding="{Binding Product, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,8">
            <Button x:Name="btnAddItem" Width="90" Height="26" Margin="0,0,8,0" Click="btnAddItem_Click">Add Item</Button>
            <Button x:Name="btnRemoveItem" Width="100" Height="26" Click="btnRemoveItem_Click">Remove Item</Button>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnSave" Width="80" Height="26" Margin="0,0,8,0" Click="btnSave_Click">Save</Button>
            <Button x:Name="btnCancel" Width="80" Height="26" Click="btnCancel_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
